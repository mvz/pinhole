#!/usr/bin/env ruby

require File.expand_path(
    File.join(File.dirname(__FILE__), %w[.. lib pinhole]))

#provider = Dir.glob("*.jpg")
provider = Pinhole::TrackerProvider.new
Pinhole::MainWindow.new(provider).run
